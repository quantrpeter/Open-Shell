# Present so packaging tools ship this folder inside the wheel, where it is
# installed as `oshell_command/`. The shell loads these files by path, not by
# import, so nothing here is executed. Files starting with `_` are skipped.
